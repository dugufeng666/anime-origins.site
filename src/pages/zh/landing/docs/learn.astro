---
/**
 * 学习手册目录页 (/zh/landing/docs/learn)。English: /landing/docs/learn。
 */
import { getCollection } from 'astro:content';
import LandingLayout from '~/components/landing/LandingLayout.astro';
import HandbookManualList from '~/components/landing/HandbookManualList.astro';
import { landingContent } from '~/config/landing';
import { siteUrl } from '~/config/site';
import { chaptersForLocale } from '~/lib/handbook';

const t = landingContent.zh;
const chapters = chaptersForLocale(await getCollection('handbook'), 'zh');

const alternates = [
  { hreflang: 'en', href: `${siteUrl}/landing/docs/learn` },
  { hreflang: 'zh', href: `${siteUrl}/zh/landing/docs/learn` },
];
---

<LandingLayout
  locale="zh"
  alternates={alternates}
  pageTitle={`${t.handbook.manuals.learn.label} | AnvilWiki 文档`}
  pageDescription={t.handbook.manuals.learn.description}
  togglePath="/landing/docs/learn"
>
  <HandbookManualList manual="learn" chapters={chapters} locale="zh" t={t} />
</LandingLayout>
